class Main extends IO {
    head : bool <- true;
    tail : int32;
    mainString : string <- "Viva la vida baby and keep on Rockin'";
    mainString2TestConcat : string <- "but rock less loudly please it is late\n";
    x : X;

    main() : int32{
        //let testConcat : string <- mainString + mainString2TestConcat in {3 };
        if head then print(mainString2TestConcat);
        x <- new X;
        x.xfun();
        printBool(isnull x);
        testString(mainString);
        tail <- 0;
        while tail < 10 do {printInt32(tail); tail <- tail + 1};
        0
    }

    testString(myString : string) : Main {
        myString <- "Viva la vida baby and keep on Rockin'";
        "lol";   
        self
    }

    truc(tail : int32, boolean : Main) : int32 { 
        tail <- tail + 3;
        tail <- tail
    } 
 }

 class X extends IO {
    xval : int32 <- 42;

    xfun() : unit {
        printInt32(xval);
        ()
    }
 }