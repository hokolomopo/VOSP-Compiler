class Main {
	head : int32;
	tail : int32;

	main() : int32{
		let n : int32 <- 3 in
			if n < 0 then {
			-1
		}else
			897
	}

	testString(myString : string) : string{
		myString <- "Viva la vida baby and keep on Rockin'";
		"lol"	
	}

 	truc(tail : int32, boolean : Main) : int32 { 
 		head <- 2 + head * tail;
 		let xs : int32 <- tail in 
 			tail <- xs;
		while true do{
			tail <- tail + 1
		};
		2 + 3	
 	} 
 }